# scraper
A landing page (GitHub Pages) + a GitHub Actions workflow that does the actual scraping. Fill in a form on GitHub, wait ~1–2 minutes, refresh the page — new contacts show up in the directory and in `results.csv`.
